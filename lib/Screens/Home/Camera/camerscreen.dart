import 'package:flutter/material.dart';

class Camerscreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
   return Scaffold(
     body: Center(child: Text("Camera"),),
   );
  }

}